version: '3.5'

services:
  app:
    container_name: penduangular_app
    build:
      context: .
      dockerfile: Dockerfile
    expose:
      - 80
    env_file:
      - .env

networks:
    default:
       external:
         name: webproxy

